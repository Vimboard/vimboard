foo:
  var1: none
  var2: none

logging:
  level:
    root: OFF

spring:

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/databaseName
    username: username
    password: password

  main:
    banner-mode: 'OFF'
